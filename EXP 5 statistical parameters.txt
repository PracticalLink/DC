EXPT: 5 
clc();
clear();
close();
count=csvRead('count.dat');
for i=1:3
 scf();
 mu(i)=mean(count(:,i));
 sigma(i)=stdev(count(:,i));
 var(i)=variance(count(:,i));
 histplot(24,count(:,i));

 xtitle('Histogram of traffic '+string(i));

end
MeanTotal=mean(mean(count));
disp('mean for indivisual traffic insertion is=',mu);
disp('standard deviation traffic insertion is=',sigma);
disp('variance of traffic insertion is=',var);
disp('Overall Mean=',MeanTotal);